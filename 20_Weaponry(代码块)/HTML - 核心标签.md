### 核心容器（布局）

1. < div> : **万能盒子**。网页80%的标签都是它。
2. < span> : **小盒子**。用来包裹一行文字的某几个字（比如给它们变色）

### 核心内容（展示）

1. < h1> ~ < h6> : **标题**。
2. < p>: **段落**（文字）。
3. < a>: **链接**（跳转）。
4. < img> : **图片**。

### 核心交互

1. < input> : **输入框**。
2. < bytton> : **按钮**。
3. < ul> + < li> :**列表**（用来做菜单、商品列表）。